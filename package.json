{
  "name": "app",
  "scripts": {
    "build": "esbuild app/javascript/application.js --bundle --minify --sourcemap --outfile=public/packs/application.js",
    "watch": "esbuild app/javascript/application.js --bundle --sourcemap --outfile=public/packs/application.js --watch"
  },
  "private": "true",
  "dependencies": {
    "@hotwired/stimulus": "^3.2.1",
    "@hotwired/turbo-rails": "^7.2.5",
    "esbuild": "^0.17.6"
  }
}
